# 2026-07-30

## Init — project documentation bootstrapped
- Created NOTE.md (project memory) using the standard template, filled from the
  actual project (Firefox Browser MCP: `mcp-server/` + `firefox-extension/`).
- Established docs/changelog/ structure.

## Release — mcp-server published to PyPI
- Enriched `mcp-server/pyproject.toml`: author `ICWR-TEAM`, project URLs
  (GitHub repo/issues), and trove classifiers.
- Built with `uv build`; validated with `twine check` (PASSED).
- Published **`firefox-browser-mcp` v0.1.0** to PyPI
  (https://pypi.org/project/firefox-browser-mcp/). Now installable via
  `uvx firefox-browser-mcp`.
- Updated root README quick-start to use the published package (added PyPI badge
  and links).

## Repo — GitHub
- Added root `.gitignore` (Python/build artifacts + secret files).
- Initialized git history and pushed to
  https://github.com/ICWR-TEAM/Firefox-Browser-MCP.
- Secrets policy: PyPI token supplied only at publish time; GitHub PAT kept in
  local `.git/config` only. No tokens committed.

## Extension — AMO packaging tooling
- Added `firefox-extension/package.json` (`web-ext` lint/build/run/sign scripts)
  and `web-ext-config.cjs` (excludes package.json/README/node_modules/artifacts
  from the packaged .zip).
- Added `data_collection_permissions: { required: ["none"] }` to the manifest
  (now required by AMO for new extensions).
- Documented the AMO submit/sign flow (dashboard + `web-ext sign`) in the
  extension README.

## Extension — fix AMO lint warnings (v0.2.1)
- Raised `strict_min_version` 109 → **142.0** (matches the version that
  introduced `data_collection_permissions`: desktop 140 / Android 142), clearing
  the two "manifest key not supported by minimum version" warnings.
- Reimplemented `browser_eval`: replaced the `Function` constructor with
  page-context `<script>` injection (result via `postMessage`), clearing the
  `DANGEROUS_EVAL` warning. Eval now runs in the page's main world but may be
  blocked by a strict page CSP.
- `web-ext lint` → 0 errors, 0 warnings.

## Extension — fix AMO trademark rejection (v0.2.2)
- AMO rejected upload: "Add-on names cannot contain the Mozilla or Firefox
  trademarks."
- Renamed manifest `name` and `browser_action.default_title` from
  "Firefox Browser MCP" → **"Browser MCP Bridge"**; updated popup `<h1>` and
  removed "firefox" from the manifest description.
- Add-on id (`firefox-browser-mcp@incrustwerush.org`), repo name, and PyPI package
  (`firefox-browser-mcp`) are unchanged.
- Rebuilt: `web-ext-artifacts/browser_mcp_bridge-0.2.2.zip` (lint 0/0). This is
  the file to upload to AMO.

## Extension — bump version for AMO re-upload (v0.2.3)
- `web-ext sign` failed with HTTP 409 "Version 0.2.2 already exists" (0.2.2 was
  already uploaded to AMO; versions are immutable and cannot be re-submitted).
- Bumped extension to **v0.2.3** (manifest + package.json), rebuilt with lint 0/0.
- Upload/sign this file instead: `web-ext-artifacts/browser_mcp_bridge-0.2.3.zip`.

## Server + Extension — CLI args, Enable/Disable toggle, mcp<2 fix (server v0.1.1, ext v0.2.4)
- Server: added CLI args `--host`, `--port`, `--log-level`, `--version`
  (override the `FBMCP_*` env vars); applied to the bridge before `mcp.run()`.
- Critical fix: pinned `mcp<2.0.0`. `mcp` 2.0.0 removed `mcp.server.fastmcp`,
  which made the runtime import fail (affected the already-published 0.1.0).
  Now resolves to `mcp 1.29.0`; verified `--version`/`--help` and import in a
  clean venv.
- Published **firefox-browser-mcp 0.1.1** to PyPI
  (https://pypi.org/project/firefox-browser-mcp/0.1.1/), requires
  `mcp<2.0.0,>=1.2.0`.
- Extension: added a persisted **Enable/Disable** toggle in the popup. When
  disabled, the background script disconnects and stops auto-reconnecting; when
  enabled it connects and retries. `background.js` gained `enabled` state,
  `disconnect()`, `setEnabled()`, and boot/reconnect guards. Bumped to v0.2.4;
  `web-ext lint` 0/0 → `web-ext-artifacts/browser_mcp_bridge-0.2.4.zip`.
- Docs: added a "CLI options" section (server README) and an "Enable / Disable"
  section (extension README); root README mentions the toggle.

## Docs — document server port/CLI args in root README
- Added a "Server CLI options" section to the root `README.md` (`--host`,
  `--port`, `--log-level`, `--version`) with an env-var fallback table.
- Quick-start and MCP-client config now show `--port` usage; noted that a custom
  port must be mirrored in the extension popup's Bridge WebSocket URL.

## Extension — connection debug logging (v0.2.5)
- Verified server side is healthy: bridge listens on 127.0.0.1:9010 and accepts
  WebSocket clients (tested in a clean venv); `uvx firefox-browser-mcp` resolves
  to 0.1.1 and runs. So "not connecting" is operational/extension-side.
- Added console logs to `background.js` connect flow ("[MCP Bridge] connecting/
  connected/disconnected/connection error…") so users can diagnose via
  about:debugging → Inspect (background). Bumped extension to v0.2.5;
  `web-ext lint` 0/0 → `browser_mcp_bridge-0.2.5.zip`.

## Context captured in NOTE.md
- Stack: Python MCP server (`FastMCP` + `websockets`, `pyproject.toml` v0.1.1)
  and a Firefox WebExtension MV2 (v0.2.4, add-on name "Browser MCP Bridge"),
  linked by a localhost WebSocket bridge (`ws://127.0.0.1:9010`) using JSON
  `{id, command, params}` messages.
- Recorded 9 architecture decisions (WebSocket bridge, MV2 persistent
  background, response-body capture via `filterResponseData`, tab id targeting,
  `browser_eval`, PyPI publish, AMO/web-ext tooling, AMO lint fixes, AMO rename).
- Pending: no e2e test yet, ungated `browser_eval`, no tests/CI, extension not
  yet signed on AMO, no icons.
